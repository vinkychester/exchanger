import styled from 'styled-components';

export const StyledInfoBlock = styled.div`
  margin-bottom: 5px;
`;

export const StyledBlockTitle = styled.div`
  font-weight: 700;
  letter-spacing: 0.3px;
  opacity: 0.4;
  display: flex;
  align-items: center;
`;

export const StyledBlockText = styled.div`
  padding: 8px 0;
  display: flex;
  align-items: center;
  word-break: break-word;
`;

export const StyledBlockSubText = styled.div`
  font-size: 12px;
  p {
    display: inline-flex;
    opacity: 0.4;
    word-break: break-word;
  }
`;