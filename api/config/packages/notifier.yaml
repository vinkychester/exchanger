framework:
    notifier:
        chatter_transports:
        #    slack: '%env(SLACK_DSN)%'
            telegram_debug: '%env(TELEGRAM_DEBUG)%'
            telegram_info:  '%env(TELEGRAM_INFO)%'
            telegram_coin_bot:  '%env(TELEGRAM_COIN_BOT)%'
            telegram_coin_bot_info:  '%env(TELEGRAM_COIN_BOT_INFO)%'
        #texter_transports:
        #    twilio: '%env(TWILIO_DSN)%'
        #    nexmo: '%env(NEXMO_DSN)%'
        channel_policy:
            # use chat/slack, chat/telegram, sms/twilio or sms/nexmo
            urgent: ['email']
            high: ['email']
            medium: ['email']
            low: ['email']
        admin_recipients:
            - { email: admin@example.com }
